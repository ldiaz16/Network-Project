services:
  - type: web
    name: airline-route-optimizer-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.app:app --config gunicorn.conf.py
    envVars:
      - key: WEB_CONCURRENCY
        value: "1"
